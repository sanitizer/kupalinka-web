
export class Language {
    locale: string;
    displayName: string;

    constructor(locale: string, displayName: string) {
        this.locale = locale;
        this.displayName = displayName;
    }

}